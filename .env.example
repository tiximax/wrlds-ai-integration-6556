# Example environment variables
# Copy this file to `.env` (local dev) or configure env on your platform

# Frontend optional endpoint override (otherwise falls back to /api/subscribe in non-test mode)
VITE_SUBSCRIBE_ENDPOINT=

# Serverless/backend variables (set only on server-side platforms, do NOT expose to client)
SUBSCRIBE_API_KEY=

